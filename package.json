{
  "name": "6d-ops-oneteam-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "firebase/functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/public-ui & npm run dev --workspace=apps/admin-ui",
    "install-all": "npm install --workspaces"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
